# ── Kontent va shaxsiy materiallar — HECH QACHON commit qilinmaydi ──
# Mualliflik huquqi + spetsifikatsiya 10-bo'lim
books/
audios/
content/local/
*.mp3
*.m4a
*.wav

# ── Maxfiy kalitlar ──
# (.env.production istisno — unda faqat OCHIQ publishable kalit bor)
.env
.env.*
!.env.example
!.env.production

# ── Node / Vite ──
node_modules/
dist/
dist-ssr/
.vite/
*.local

# ── Editor / OS ──
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db
